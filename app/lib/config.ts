/**
 * Feature flag: when true, custom animated project showcase pages are shown
 * for supported projects (currently: docride).
 * Set to false to fall back to the generic project template for all slugs.
 */
export const SHOW_CUSTOM_PROJECT_PAGES = false;
